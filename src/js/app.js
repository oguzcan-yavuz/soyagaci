/* Pdf Yukleme */
$(document).ready(function () {
    console.log(Object.keys(gelen["details"]));
    var anneTarafi = 0;
    var babaTarafi = 0;
    for(var key in Object.keys(gelen["details"])) {
        if(key.startsWith("A")) {

        }
    }
    $('#upload form input').change(function () {
        $('#upload form p').text("Lütfen aşağıdaki 'Soy Ağacımı Göster' butonuna tıklayınız.");
    });
});


